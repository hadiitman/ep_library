package nerds.ep.advance.utils;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;

/**
 * Created by hadi on 17/02/2018.
 */

public class BootCompeleteReciever extends BroadcastReceiver {
    @Override
    public void onReceive(Context context, Intent intent) {
//        Intent dlIntent = new Intent(context , DownloaderService.class);
//        context.startService(dlIntent);
    }
}
